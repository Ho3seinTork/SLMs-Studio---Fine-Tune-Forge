fastapi>=0.110
uvicorn[standard]>=0.29
pywebview>=4.4.1
psutil>=5.9
python-multipart>=0.0.9

# --- Deep learning / fine-tuning libraries ---
# torch is intentionally NOT pinned here because the right build depends on
# your hardware (CPU-only vs CUDA). Install it separately first - see README.
transformers>=4.42
peft>=0.11
trl>=0.9
datasets>=2.19
accelerate>=0.30
sentencepiece
protobuf

# --- Optional: only needed for 4bit QLoRA on an NVIDIA GPU with CUDA ---
# bitsandbytes>=0.43
